#include<bits/stdc++.h>
using namespace std;
int getMinDuration(vector<vector<int>> &G)
{
    vector<bool> visited(n,false);
    queue<int> q;
    q.push(0);
    visited[0]=true
    while(!q.empty())
    {
        
    }
}
int main()
{
    int n;
    vector<int> G(n);
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            int val;
            cin>>val;
            G[i].push_back();
        }
    }
    cout<<getMinDuration(G);
    return 0;
}